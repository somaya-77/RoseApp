// 'use client'

// import {AuthForm} from "@/components"
// import { useOTPHook } from "@/hooks/use-otp"
// import { otpInputs } from "@/lib/constance/constance"
// import { otpFormValues } from "@/lib/schemas/otp.schema"

// export default function OTPForm({email}: {email?: string | undefined | null}) {
//     const { form, onSubmit } = useOTPHook()
//     return (
//         <AuthForm<otpFormValues> title="Verify OTP" data={otpInputs} titleBtn="Verify Code" handleSubmit={form.handleSubmit(onSubmit)} form={form} path="" description="Please enter the 6-digits code we have sent to:" email={email}/>
//     )
// }


