// 'use client'

// import {AuthForm} from "@/components";
// import { confirmInputs } from "@/lib/constance/constance";
// import { useCreatePasswordHook } from "@/hooks/use-create-password";
// import { createPasswordFormValues } from "@/lib/schemas/create-password.schema";

// export default function CreatePasswordForm() {
//     const { form, onSubmit } = useCreatePasswordHook()

//     return (
//         <AuthForm<createPasswordFormValues> title="Create a New Password" data={confirmInputs} titleBtn="Reset Password" handleSubmit={form.handleSubmit(onSubmit)} path="" form={form} />
//     )
// }
